---
"@savvy-web/pnpm-plugin-silk": patch
---

## Dependencies

| Dependency | Type | Action | From | To |
| --- | --- | --- | --- | --- |
| `@biomejs/biome` | dependency | updated | 2.5.10 | 2.5.11 |

The `lint` and `lint:peers` catalogs pin Biome to 2.5.11, so every repo consuming this config dependency picks the upgrade up on its next install.
