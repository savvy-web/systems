---
"@savvy-web/templates": patch
---

## Maintenance

- The changeset config template now writes the `$schema` URL `https://unpkg.com/@changesets/config@4.0.0-next.6/schema.json` (was `@changesets/config@3.1.1`), keeping generated `.changeset/config.json` output in sync with the v3 config schema.
