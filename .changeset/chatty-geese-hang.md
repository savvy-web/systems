---
"@savvy-web/pnpm-plugin-silk": minor
---

## Features

- Adds `@tsdoctor/*` packages to `minimumReleaseAgeExclude` to enable new `rspress-plugin-api-extractor` dependnecy installs in sync with the plugin.
- Removes old effect packages that migrated to `@effected` from `minimumReleaseAgeExclude`