---
"@savvy-web/cli": patch
---

## Maintenance

* `savvy changeset init` now generates `.changeset/config.json` pointing at the stable `@changesets/config@4.0.0` schema instead of the `4.0.0-next.6` prerelease
