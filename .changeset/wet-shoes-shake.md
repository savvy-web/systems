---
"@savvy-web/pnpm-plugin-silk": minor
---

## Features

- Bumps the tooling catalogs: `@rsbuild/core` to `^2.2.8`, `@types/node` to `^26.6.2`, `rspress-plugin-mermaid` to `^1.0.3`, and `turbo` to `^2.11.2` (range and peer).
