---
"@savvy-web/tsdown-plugins": patch
---

## Refactoring

* Replaced `sort-package-json` with `@effected/package-json`'s `PackageJsonFormat.sortValue` in manifest transformation (byte-identical output)
