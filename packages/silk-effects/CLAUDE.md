# @savvy-web/silk-effects

Shared Effect library for the Silk Suite, and the home of the dev-tooling business logic consumed by `cli`, `mcp`, and `silk`. Single root export, dual-format ESM+CJS.

## Key surface

- One entry point (`.`) exporting six namespaces — `Changesets`, `Commitlint`, `Lint`, `PrBody`, `Turbo`, `Repos` — plus standalone services, schemas, and tagged errors.
- `PrBody` is the shared PR-body contract (savvy-web/systems#419): the FROZEN `silk-release:` marker grammar (`Markers` — the token names the contract, not the emitting action; never parameterize or rename it, every live PR body and the `pr-body` plugin skill key on the literals), the generic `Region` pair grammar, `ManagedPrBody` (build/upsert/extractSummary/extractReferences with owned-id subtraction), `ClosingReferences` (the two spellings — comma-joined trailer for commitlint, bare per-line for GitHub's linker; neither consumer accepts the other's), `LinkedIssueRef.isClosed` (the ONLY sanctioned closedness test — case-insensitive, so GraphQL `CLOSED` classifies), and advisory `PrBodyDiagnostic.scan`. Everything is pure and total (malformed input degrades fail-safe, never fails); byte-parity with `silk-release-action`'s `pr-body.ts` is pinned by `__test__/fixtures/pr-body/expected.json`, and `__test__/pr-body/skill-sync.test.ts` drift-lints the plugin skills against the exported literals.
- `Turbo` is a read-only Turborepo inspection namespace: `TurboInspector` service (needs `ToolDiscovery` from `@effected/commands`) + `TurboDigest` pure transforms (`diagnoseCache`/`taskGraph`/`affected`), all `--dry`, never executes tasks.
- `Repos` owns the vendored `.repos/` mechanism through four services: `ReposConfigStore` (the `.repos/config.json` manifest; every mutation goes through its lock-serialized `update`), `ReposManager` (`status`/`sync`/`add`/`pin`/`note`/`remove`/`rename`/`restore`/`deregister` submodule plumbing over `@effected/git`; `deregister` clears a stale `submodule.<section>` registration from the superproject's LOCAL config — local config only, no lockdown bracket, nothing staged — and refuses a section that still backs a live manifest entry), `ReposDrift` (read-only `check`, reconciling FIVE authorities into TEN drift kinds behind `savvy repos status --drift`) and `ReposLockdown` (the boundary). Load the design doc before changing any of it; these invariants must not be broken blind:
  - **The lock covers the WORKTREE only** — files `0444` / dirs `0555`, each file's executable bit preserved, symlinks skipped entirely (no `lchmod` on Linux). The submodule's git metadata dir is deliberately NOT locked: a read-only gitdir breaks `git pull`'s default submodule recursion (it cannot write `FETCH_HEAD`) and any client keeping per-gitdir state (GitKraken's `gk/`). `walkRoot` is ASYMMETRIC ON PURPOSE and the asymmetry IS the migration — `unlock` still walks the gitdir (freeing one an older version locked), `lock` never re-locks it, so one `savvy repos sync` migrates a checkout. Do not "fix" that.
  - The worktree lock blocks file edits and `git reset --hard` but NOT `git checkout <other>` (verified, git 2.54), so the invariant is "a drifted pin is always detected and one command from repaired" (`checkoutDiverged` → `restore`), NOT "the pin cannot drift".
  - **The declarative half is written by BOTH `sync` and `add`**: `submodule.<path>.update = none` and `fetch.recurseSubmodules = false` into the superproject's LOCAL config, never `.gitmodules`. `update = none` also makes `git submodule update --init` skip, so `sync`'s initialize call passes `checkout: true` (`--checkout`, git's documented command-line override — landed in `@effected/git` 0.8.0); the key itself is never flipped, and `add` runs no `submodule update` at all. Never set `active = false` — an inactive submodule reads as uninitialized, so drift would report `missingWorktree` for every repo, and `git submodule init` flips it back anyway.
  - The permissions are the boundary; the silk plugin's Bash/fs/MCP guards are early-warning UX in front of them, never a substitute. A hand `chmod` survives only until the next `sync`/`pin`/`restore`. Reads work unmodified against a locked tree.
  - `sync`/`add`/`pin`/`remove`/`restore` bracket their git mutations in `withUnlocked`, which owns the whole relock finalizer contract (a relock failure surfaces as `ReposLockdownError` only when it is the SOLE failure); `rename` hand-rolls the same contract because it unlocks `oldName` and must relock `newName`. Lock recursion order is direction-dependent (lock chmods a dir AFTER recursing, unlock BEFORE) — inverting it fails with `EACCES`.
  - Reports state what was ACHIEVED, not attempted: `ReposSyncReport.boundaryMarked`, `ReposRestoreResult.stillDirty` (`restore` re-reads `git status` after the reset), `ReposRemoveResult.removedEntry` — hand its `orientation` back to `add`'s `orientation` option to make a re-vendor lossless. `sync`/`restore` deinit a vendored repo's own submodules first, since sparse-checkout cannot evict a nested checkout. The deprecated `RepoStatusEntry.commit` alias is GONE; read `stagedCommit`.
  - Every `manifest.repos[name]` membership read goes through the `Object.hasOwn`-guarded `getRepoEntry` helper, never a bare bracket read — a manifest key literally named `"constructor"` must fail typed as `RepoNotFoundError`.
- `Changesets` holds `ConfigInspector`, `BranchAnalyzer`, `ChangesetLinter`, `ReleasePlanner` and `DepsRegen`.
- `ReleasePlanner` (`plan`/`preview`/`apply`) drives the changesets v3 engine directly — no v1 packages adapter, and `apply` does native, dry-run-aware version bumping. `preview` and `apply` both take a `changelogModules` option mapping changelog ids to module paths for no-`node_modules` callers, sharing one `withChangelogModules` rewrite; `plan` renders nothing so it needs none. Map membership is `Object.hasOwn`, NEVER `map[id] === undefined` — an id naming an `Object.prototype` member reads back an inherited function through the latter and skips the typed unmapped-id error.
- `DepsRegen` splits `plan`/`execute` and owns dependency-changeset orchestration. It snapshots both diff sides via `@effected/workspaces`' `WorkspaceSnapshots` (the kit renamed this from `PointInTimeWorkspace` — that name is gone, do not reintroduce it), resolving `catalog:`/`workspace:` per ref BEFORE diffing so equal-resolved rows are suppressed, and drops `devDependency` rows. Gating is "versionable minus ignored" — in scope when `publishable OR privatePackages.version` AND not on the changeset ignore list (ignore wins over `--package`). A pure dependency changeset is deleted ONLY when its package is in scope AND rewritten this run (present in `toWrite`) AND the file was authored on this branch rather than already committed at the merge-base ref (`gitListChangesetFilesAtRef`) — either gap destroys a still-relevant release note with nothing recreated (#258). `plan()` refreshes `ConfigInspector` and `ChangesetConfig` first, because both hold per-root caches that never self-expire and a long-lived host (the `savvy-mcp` server keeps one `DepsRegen` per process) would otherwise miss `.changeset/config.json` edits between calls (#229). File I/O runs through `FileSystem` (`ChangesetIOError`). `DepsRegen.layer` requires `WorkspaceSnapshots | ConfigInspector | WorkspaceDiscovery | PublishabilityDetector | ChangesetConfig | Git | FileSystem`; `DepsRegenDefault` supplies silk's opinionated defaults and leaves only `FileSystem | Path | ChildProcessSpawner` open — `WorkspaceSnapshots` reads git history, so give it a spawn-capable platform layer (`NodeServices.layer`), never a filesystem-only one.
- `ConfigInspector` file attribution has a fixed precedence: (1) directory containment, deepest `workspaceDir` wins; (2) `additionalScopes`; (3) `versionFiles`; (4) a root-as-package fallback. A scope whose `workspaceDir` IS the project dir is held OUT of the containment pass and applied only at step 4 — every file is "inside" the root by definition, so competing on containment let it shadow the `additionalScopes`/`versionFiles` a config declared. Keep that ordering when touching `classifyOne`. Step 4 stays containment-checked, so a `../` or absolute path outside the repo stays unmapped. Its release-surface fallback treats EVERY workspace package as a release surface when the config sets `privatePackages.version: true`, per the `ChangesetConfig.versionPrivate` truth-table.
- `ChangesetLinter.validateContent` now enforces the dependency-table format (CSH005) via the remark `DependencyTableFormatRule`; prose `## Dependencies` sections are rejected.
- `VersionFiles.updateFile` patches version files with format-preserving minimal edits (jsonc-effect `modify` + `applyEdits`), so bytes outside the edited value are untouched and JSONC survives; never reintroduce a `JSON.parse`/`JSON.stringify` round-trip. Missing wildcard-free leaf properties are inserted with detected indent; wildcard paths update existing matches only. All of its I/O goes through the Effect `FileSystem` service — no `node:fs` left, so `discoverVersions`, `updateFile` and both `process*VersionFiles` members are Effects requiring `FileSystem.FileSystem`, and `JSON.parse` sits inside `Effect.try` because a throw in `Effect.gen` is a defect the fail-soft `orElseSucceed` would not catch. The two process entry points differ in ERROR POSTURE on purpose and must not be re-unified: shared private `applyOne` fails typed, `processVersionFiles` (legacy) pipes `Effect.orDie` so a per-file failure stays a DEFECT matching the synchronous throw it had under `node:fs`, while `processResolvedVersionFiles` stays TYPED because `ReleasePlanner.apply` maps it to `ReleasePlanError` — a defect there bypasses the inspector's catch and crashes `apply()`.
- Changelog rendering: release lines carry NO commit-link prefixes (git history is the reference — do not re-add them), dependency tables emit their own `### Dependencies` heading, and changeset-less releases (fixed/linked co-bumps) render a `### Maintenance` note via `deriveMaintenanceReason` + `MaintenanceNotePlugin` (parameterized per version block by `ReleasePlanner`, deliberately not a preset member). `ChangelogTransformer` runs the full `SilkChangesetTransformPreset`.
- `Lint` handlers have TWO entry points — the lint-staged `create()` command and the corresponding `savvy lint fmt <name>` subcommand — so any formatting step must be a public static both call, never inlined in one. `Lint.PnpmWorkspace.formatContent` is the worked example: `@effected/yaml` (`quoteStyle: "double"` + `indentSequences: true`) now emits the repo's byte format directly, so there is no Prettier post-process. `Lint.Yaml` runs on `@effected/yaml` too as of 0.8.0 (effected#127 landed), so Prettier and `yaml-lint` are gone from the package entirely. Its statics are SYNC — the engine is a pure IO-free tier — and `validateFile` gates on `Yaml.parseAllResult`, so a multi-document file whose LATER document is invalid is rejected rather than silently accepted. There is no config-file tier: the removed `.yaml-lint.json` discovery never resolved, and formatting is configured in code via `YamlOptions.format`. Because that option has to survive the process hop to the `fmt` subcommand, `fmtCommand` serializes it onto the command line (`encodeFormatOptions` / `--format` / `parseFormatOptions`) — a shared static alone does NOT keep the two entry points in agreement once the format step takes options.
- `Commitlint`'s `silk/body-no-markdown` rule detects bold via the asterisk form only (`**text**`), so `__SNAKE_CASE__` identifiers are not flagged. `silk/body-prose-only` exists but `createConfig` never enables it, so dash bullets are legal — check the preset before claiming a rule is enforced. `verbosity`'s exported `VERBOSITY_LINE_THRESHOLD`/`VERBOSITY_WORD_THRESHOLD` (12/150) encode the squash-merge brevity shape the plugin's `commit-create` skill teaches; move the two together. `closes-trailer`'s `hasClosingTrailer` is one expression over the kit's `parseClosingLists`, so one comma-joined `Closes #a, #b` line matches every id; it takes all nine of GitHub's closing tenses and is strictly whole-line — a keyword mid-prose does NOT satisfy the rule, deliberately (a trailer is a line of its own; do not "fix" it onto the inline harvest).
- `SilkPublishability.detect(pkgName, raw, binding)` resolves publish targets from the bundler's `dist/prod/targets.json` binding (Record-map `publishConfig.targets` form); public API `readTargetsBinding` + types `TargetsBinding`/`TargetBinding`/`TargetGroupBinding`.
- `SilkWorkspaceAnalyzer` is the composite workspace service (four layer deps). The shared husky-hook shells are DATA now: `SavvySections` (`SavvyBaseSection`/`SavvyHooksSection`/`savvyToolSection`) defines them and the kit's `ManagedSection` engine renders them; `SectionId` keys uppercase, so keep the marker-compat guard when adding a section.
- The kit owns the mechanisms this package used to — do NOT re-add them here, and read the design doc's migration table before hunting a service a stale note mentions: `VersioningStrategy`/`TagStyle`/`ReleaseTag` → `@effected/workspaces` (value classes; `classify` is pure and total, so there is no error to catch), `ToolDiscovery`/`Tool`/`Run` → `@effected/commands` (set env with `Run.extendEnv`, never bare `setEnv`), `ManagedSection`/`Section`/`CommentStyle` → `@effected/templates`; the GitHub issue-reference grammar → `@effected/github-references` (`parseBareLines`/`parseClosingLists`/`collectReferenceLists`/`keywordFamily`), which retired all three hand-rolled copies: `ClosingReferences.BARE_LINE_PATTERN` is GONE from the public surface (call `parseBare`), and `parseIssueReferences` now requires the `#` and accumulates every list in the message rather than the first per category. Never re-hand-roll a keyword or `#N` pattern here.
- `src/index.ts` re-exports NOTHING from the kit: consumers import kit surfaces from `@effected/*` directly, so one type never gets two import paths.
- Result types are Effect `Schema` as the single source of truth, with derived interfaces.
- All Effect code uses class-based `Context.Service` (each with a companion `*Shape` interface), `Schema.Class`/`Schema.TaggedClass`, `Data.TaggedError`.

## Design

Load for service patterns, value-object conventions, layer composition, and the full service inventory:
→ `@../../.claude/design/silk-effects/architecture.md`
Load when implementing a new service, changing a result schema, touching the `Repos` namespace (its Vendored Repos section covers all four services plus the lifecycle-operations and drift-reconciliation subsections), or onboarding a consumer repo. Its testing-strategy section covers the fixture-tree integration tests, the `TestConsole`-vs-`process.stderr` discrimination that decides whether a logging test can stay on `it.effect`, the memfs carve-out (the `ReposLockdown` blocks and `services__config-store.test.ts`'s `it.live` lock tests stay on real tmpdirs because memfs records mode bits without enforcing them — do not "finish the migration"), and the test-only module-export convention (helpers exported from their own module but deliberately never from the index — do not "tidy" them into it).
