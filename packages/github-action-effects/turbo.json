{
	"extends": ["//"],
	"tasks": {
		"build:dev": {
			"inputs": ["$TURBO_EXTENDS$", "savvy.build.ts", "!rslib.config.ts"],
			"outputs": ["dist/dev/**"]
		},
		"build:meta": {
			"inputs": ["$TURBO_EXTENDS$", "savvy.build.ts"]
		},
		"build:prod": {
			"inputs": ["$TURBO_EXTENDS$", "savvy.build.ts", "!rslib.config.ts"],
			"outputs": ["dist/prod/**"]
		}
	}
}
