---
id: packages/github-action-effects/api/variable/globlive
title: "GlobLive — github-action-effects variable"
summary: "Live implementation of Glob using `node:fs.globSync`, `node:crypto` and `node:fs.createReadStream`. No dependency on `@actions/glob`."
tier: packages
source: generated
tags: [github-action-effects, api]
priority: 0.3
related: []
---

# GlobLive

Live implementation of [Glob](silk://packages/github-action-effects/api/class/glob) using `node:fs.globSync`, `node:crypto` and `node:fs.createReadStream`. No dependency on `@actions/glob`.

```ts
GlobLive: Layer.Layer<Glob>
```
