---
id: packages/silk-effects/api/type/rawpublishtargets
title: "RawPublishTargets — silk-effects type"
summary: "The `publishConfig.targets` map, keyed by target id (`npm`, `github`, or a custom key)."
tier: packages
source: generated
tags: [silk-effects, api]
priority: 0.3
related: []
---

# RawPublishTargets

The `publishConfig.targets` map, keyed by target id (`npm`, `github`, or a custom key).

```ts
type RawPublishTargets = Record<string, RawTargetValue>;
```
