# Auto-allow MCP operation suffixes for any GitHub MCP server
# (mcp__github__<op> or mcp__github-<scope>__<op>).
# === Read ===
get_me
get_file_contents
list_branches
list_commits
list_issues
list_pull_requests
pull_request_read
issue_read
search_issues
search_pull_requests
search_code
list_releases
get_latest_release
get_release_by_tag
get_tag
list_tags
get_repository_tree
# === Write ===
add_issue_comment
add_reply_to_pull_request_comment
update_pull_request
update_pull_request_branch
# NOTE: `create_pull_request` is auto-allowed without commit-message validation.
# The plugin validates `git commit` messages (and `gh pr create|edit` titles via
# the Bash cold path); MCP-driven PR creation is treated as a coarse-grained
# operation rather than a commit event. If you need PR-title validation via MCP,
# remove this entry and add a cold-path handler.
create_pull_request
create_branch
